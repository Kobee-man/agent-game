# 阶段一执行计划：基础设施层

> **目标**: 搭建 LangChain + PGVector + Embedding 基础，确保 LLM 调用、向量化、向量检索三大能力跑通
> **原则**: 遵循 code-simplifier 规范（函数 < 50 行、参数 ≤ 4、单一职责、早期返回）

---

## 环境确认

| 项目 | 状态 |
|------|------|
| PostgreSQL 18.4 | ✅ 运行中，端口 5432 |
| pgvector 扩展 | ✅ 已安装 |
| psycopg2-binary 2.9.12 | ✅ 已装（SQLAlchemy 用） |
| httpx 0.28.1 | ✅ 已装（需确认是否还要保留） |

---

## 任务拆分（7 步，逐步审核）

### T1: 更新 requirements.txt — 添加 LangChain / Embedding / PGVector 依赖

**操作**:
- 新增 `langchain-core`, `langchain`, `langchain-community`
- 新增 `langchain-deepseek`, `langchain-ollama`
- 新增 `langchain-huggingface`, `sentence-transformers`
- 新增 `langchain-postgres`, `pgvector`
- 新增 `psycopg[binary]` (psycopg3, langchain-postgres 需要)
- 保留 `psycopg2-binary` (SQLAlchemy 仍用 v2)
- 保留 `httpx` (确认是否有其他地方使用)
- 新增 `langchain-text-splitters`

**审核点**: 依赖列表是否完整、有无冲突

---

### T2: 批量安装依赖 + 验证导入

**操作**:
- `pip install -r requirements.txt`
- 逐个验证 `import` 能成功

**审核点**: 安装是否成功、有无版本冲突

---

### T3: 更新 config.py — 新增 LangChain / Embedding / 向量库配置

**操作**: 在 `core/config.py` 新增配置项：
- `LLM_TEMPERATURE`, `LLM_MAX_TOKENS`
- `EMBEDDING_MODEL`, `EMBEDDING_DEVICE`
- `VECTOR_COLLECTION_PREFIX`, `VECTOR_SEARCH_TOP_K`, `VECTOR_DISTANCE_THRESHOLD`

**审核点**: 配置项命名、默认值是否合理

---

### T4: 新建 core/llm_factory.py — LangChain LLM 工厂

**操作**: 创建 LLM 工厂模块
- `create_llm(mode)` → 返回 ChatDeepSeek 或 ChatOllama
- `create_llm_with_fallback()` → 主 LLM + 备用降级

**审核点**: 接口设计、降级逻辑、代码简洁度

---

### T5: 改造 core/llm_service.py — 兼容层

**操作**: 将现有 `llm_service.py` 改为 LangChain 薄封装
- 保留 `llm_service.chat(prompt, system_prompt)` 旧接口
- 内部转为 LangChain messages 格式调用
- 保留 `llm_service.is_available()` 接口
- 全局单例 `llm_service` 保持不变

**审核点**: 接口兼容性、现有代码是否能无改动继续使用

---

### T6: 新建 core/embedding.py — Embedding 模型管理

**操作**: 创建 Embedding 管理模块
- `get_embedding()` → 懒加载 HuggingFaceEmbeddings 单例
- 模型: BAAI/bge-small-zh-v1.5，CPU 运行

**审核点**: 单例模式、加载时机、错误处理

---

### T7: 新建 core/vector_store.py + 验证向量库

**操作**:
- 创建 `core/vector_store.py`：`get_vector_store(collection_name)` → PGVector 实例
- 写入测试数据 → 检索验证
- 更新 `.env` / `.env.example` 新增配置项

**审核点**: 连接串格式、集合命名、检索结果正确性

---

## 验收标准

- [ ] `python -c "from core.llm_factory import create_llm; llm = create_llm(); print(llm.invoke('你好').content)"` 返回正常
- [ ] `python -c "from core.embedding import get_embedding; e = get_embedding(); print(len(e.embed_query('测试')))"` 返回 512 维
- [ ] `python -c "from core.vector_store import get_vector_store; vs = get_vector_store('test'); vs.add_texts(['hello']); print(vs.similarity_search('hello'))"` 返回结果
- [ ] 所有现有 API 接口不受影响（main.py 能正常启动）
- [ ] PostgreSQL 中出现 pgvector 相关表
